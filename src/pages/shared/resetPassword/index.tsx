import * as React from 'react'
import { RouteChildrenProps } from 'react-router'

export function ResetPasswordPage(props: RouteChildrenProps) {

    // ---------------------------------------------
    // Transformations
    // ---------------------------------------------
    // Render

    return (
        <div className={'dc-reset-password-page'}>
            Reset de senha
        </div>
    )
}